/**
 * ═══════════════════════════════════════════════════════════════════════════════
 * 📁 ملف: stores/index.ts
 * ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 * 📝 وصف: ملف التصدير الرئيسي للمخازن
 * 🔧 الغرض: توفير واجهة موحدة للوصول إلى جميع المخازن
 * ═══════════════════════════════════════════════════════════════════════════════
 */

// ═══════════════════════════════════════════════════════════════════════════════
// 📦 تصدير مخزن واجهة المستخدم
// ═══════════════════════════════════════════════════════════════════════════════
export {
  useUIStore,
  useSidebar,
  useModal,
  usePanel,
  useBottomPanel,
  useTabs,
  type UIState,
  type UIActions,
  type ModalId,
  type PanelId,
  type SidebarView,
  type ModalState,
  type PanelState,
} from './ui-store';
