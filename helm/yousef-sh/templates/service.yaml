apiVersion: v1
kind: Service
metadata:
  name: {{ include "yousef-sh.fullname" . }}
  labels:
    {{- include "yousef-sh.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "yousef-sh.selectorLabels" . | nindent 4 }}
